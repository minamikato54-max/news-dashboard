# ニュースダッシュボード 仕様書

作成日：2026-05-03

---

## 0. アクセスリンク

| 用途 | URL |
|------|-----|
| **毎日のニュース閲覧（推奨）** | https://minamikato54-max.github.io/news-dashboard/ |
| リポジトリ | https://github.com/minamikato54-max/news-dashboard |

> 毎朝7時（JST）以降にブラウザで開くと最新ニュースが表示されます。

---

## 1. プロジェクト概要

複数のニュースソースから日本語ニュースを自動収集し、AI要約・用語解説・クイズ付きで毎朝GitHub Pagesに公開する自動化システム。課題共有にも利用できるよう、URLひとつで誰でも閲覧可能にする。

---

## 2. 要件

### 基本要件
- 毎朝7時に自動実行
- 日本語ニュースのみ
- 4カテゴリ × 4記事 = 計16記事/日
- 記事タイトル・要約・元記事リンクを表示
- GitHub Pages で公開（ログイン不要・URLのみで閲覧可）

### カテゴリ
| カテゴリ | ニュースソース（優先順） |
|----------|----------------|
| 経済 | NHK経済RSS → Google News RSS（「経済」検索） |
| AI事情 | Google News RSS（「AI 人工知能」検索）→ Google News RSS（「生成AI 最新」検索） |
| 国内 | NHK社会RSS → NHK政治RSS |
| 海外 | NHK国際RSS → BBC日本語RSS → Google News RSS（「海外 国際 ニュース」検索） |

※ 前のソースで4件に満たない場合、次のソースで補充（フォールバック方式）

---

## 3. 機能一覧

### 3-1. 毎日更新機能（コア）
- RSSフィードから各カテゴリ4記事を取得
- ChatGPT APIで各記事を2〜3行で要約
- 結果をGitHub Pagesに自動反映

### 3-2. 用語解説
- 各記事中の難しい単語・専門用語をChatGPTが自動抽出
- 簡単な説明文を生成して記事に付記

### 3-3. クイズ機能
- 各記事の内容をもとにChatGPTが4択クイズを自動生成
- ページ内でインタラクティブに回答できる（JavaScript）

### 3-4. キーワード検索フィルター
- ページ内の検索ボックスにキーワードを入力すると即時フィルタリング
- クライアントサイドJavaScriptで実装（サーバー不要）

### 3-5. アーカイブ機能
- 毎日の記事データを `data/articles/YYYY-MM-DD.json` としてリポジトリに保存
- archive.htmlで過去30日分のニュースを日付・カテゴリで閲覧可能

### 3-6. 週次まとめ
- 毎週日曜7時に自動実行
- `data/articles/` の直近7日分JSONから記事を読み込み
- ChatGPTで週の重要ニュースをまとめた要約を生成
- weekly.htmlに反映

### 3-7. 音声読み上げ
- 記事ごとに「🔊 読み上げ」ボタンを設置
- ブラウザ標準の Web Speech API（SpeechSynthesis）で実装（外部APIキー不要）
- 読み上げ順：**要約 → 「クイズです。〔問題文〕」**（クイズがある記事のみ）
- 読み上げ中はボタンを「⏹ 停止」に切り替え。クリックで即時停止
- `preprocessTTS()` 関数で読み誤りを補正（2026-05-21 拡張）
  - `〔漢字〕力` → `〔漢字〕りょく`（例：競争力→きょうそうりょく）
  - `（注釈）` → `、`（全角括弧注釈をポーズに変換）
  - `・` → `、`（中点をポーズに変換）
  - `30%` → `30パーセント`
  - `→` → `から`、`↑` → `上昇傾向の`、`↓` → `下降傾向の`
  - `【】「」『』` → 削除（強調括弧を除去）
- `loadVoice()` 関数で日本語女性音声を優先選択
  - 選択順：Microsoft Haruka → Google 日本語 → Kyoko → O-Ren → 任意の ja-JP 音声
  - 読み上げ速度：0.95（やや遅め・聞き取りやすさ重視）
- `speechSynthesis.onvoiceschanged` で音声一覧の非同期読み込みに対応

### 3-8. お気に入り保存
- 記事ごとに「お気に入り」ボタン（★）を設置
- ブラウザの localStorage に記事IDを保存（サーバー不要）
- お気に入り一覧をフィルタリングで表示できる

### 3-9. SNSシェア
- 記事ごとにX（Twitter）・LINEへのシェアボタンを設置
- 記事タイトル＋GitHubPagesのURLをパラメータに埋め込んだリンクを生成
- 静的リンクのみで実装（JavaScript不要）

### 3-10. トレンドワード可視化
- `data/articles/` の直近7日分JSONから記事タイトル・要約を集計
- ChatGPTで上位トレンドワードを抽出
- weekly.html内に横棒グラフ（Chart.js）で表示

### 3-11. 連続閲覧ストリーク
- ページを開いた日付をlocalStorageに記録
- 連続で開いた日数をページ上部に表示（例：「🔥 3日連続閲覧中！」）
- 1日でも途切れるとリセット

### 3-12. 読了チェック
- 記事ごとに「読んだ」ボタンを設置
- チェック済み記事はlocalStorageに保存
- 今日の16記事のうち何記事読んだか進捗バーをページ上部に表示
- 翌日（日付が変わったタイミング）に自動リセット

### 3-13. 重要度スコア
- ChatGPT APIで各記事に重要度（★1〜3）を自動判定
- 記事カードに重要度バッジを表示
- ★3の記事は視覚的に強調表示

### 3-14. クイズ正答率の記録
- クイズの正解・不正解をlocalStorageに日付・カテゴリ付きで保存（`nd_quiz_YYYY-MM-DD`）
- archive.html最上部にカテゴリ別折れ線グラフ（Chart.js）で日別推移を表示
- グラフ上部に本日の正答率をカテゴリ別テキストで表示
- クイズ回答時にカテゴリをlocalStorageに一緒に保存することで、記事IDに依存しない正確な集計を実現

### 3-15. 背景知識の補足
- 記事ごとに「背景を知る」ボタンを設置（デフォルト非表示）
- ボタンを押したときのみChatGPT生成の背景知識（1〜2行）をアコーディオン展開
- 生成内容はHTML生成時に埋め込み済み（ページロード後の追加API呼び出しなし）

### 3-17. バッジシステム
- クイズ正解累計数に応じてバッジを自動付与（localStorage に保存）
- バッジ獲得時にトースト通知（画面上部に3秒表示）
- 獲得済みバッジはページ上部のバッジバーに常時表示

| 正解数 | バッジ |
|--------|--------|
| 1問 | 🎯 初正解 |
| 5問 | 📚 5問正解 |
| 10問 | ⭐ 10問達成 |
| 20問 | 🌟 20問達成 |
| 50問 | 👑 クイズ王 |

- localStorage キー：`nd_badges`（獲得バッジ名の配列）

### 3-18. 一言メモ
- 各記事カードに「📝 メモ」ボタンを設置
- クリックで textarea が展開（再クリックで折りたたみ）
- 入力と同時に `localStorage` に自動保存（`nd_memo_<記事ID>`）
- メモ保存済みの場合はボタンラベルが「📝 メモあり ✓」に変わる
- メモ内容は記事IDに紐づくため、記事が変わる翌日以降は別のキーとして蓄積

### 3-19. 昨日の復習
- ページロード時に前日間違えたクイズを自動検出し、ページ上部（タブより上）に表示
- 表示条件：前日の `nd_quiz_DATE` に `ok:false` のエントリが存在する場合
- 復習カードで再回答可能（正誤フィードバックあり・バッジ集計対象外）

**データ保存（クイズ回答時に自動実行）：**
- `nd_quiz_DATE`：正解/不正解の記録（既存）
- `nd_quiz_detail_DATE`：復習用の問題詳細（question / choices / answer_index / category / title）

### 3-20. 今日の3行日記
- 右下固定の「📔」FABボタン（オレンジ）でモーダルを開く
- 3つのテキストエリアに沿って振り返りを記録

| # | 入力欄 |
|---|--------|
| 1 | 今日一番印象に残ったニュースは？ |
| 2 | そのニュースについて感じたこと・考えたことは？ |
| 3 | 明日確認したいこと・もっと知りたいことは？ |

- 「💾 保存」で `nd_diary_DATE` に保存（JSON配列）
- モーダル内に過去5日分の日記履歴を表示

### 3-21. フラッシュカード
- 右下固定の「🃏」FABボタン（紫）でモーダルを開く
- 今日の全記事の用語解説（`terms`）をDOMから収集・シャッフルして出題
- カード表面：単語、クリックで裏面（解説）にフリップ
- 「✅ 覚えた」→ 次のカードへ進む
- 「🔄 もう一回」→ 現在のカードを末尾に移動して再出題
- 全カード終了時に完了メッセージを表示
- 用語が0件の日は「用語解説がありません」とアラート表示

### 3-16. 関連過去記事リンク
~~- Supabaseのアーカイブから同カテゴリ・類似キーワードの過去記事を自動抽出~~
~~- 記事カード下部に「関連記事」として最大2件リンク表示~~
~~- weekly.yml実行時にChatGPTで類似度を判定して紐付け~~

> **2026-06-01 廃止：** Supabase廃止に伴い `related_ids` 更新処理を削除。カードに関連記事は表示されない。

---

## 4. システム構成

```
【毎朝7時 / GitHub Actions】
RSS取得（NHK + Google News）
    ↓
ChatGPT API（記事ごとに一括処理）
  ├─ 2〜3行要約
  ├─ 用語解説（難しい単語を自動抽出・説明）
  └─ クイズ問題生成（4択）
    ↓
data/articles/YYYY-MM-DD.json に保存（リポジトリ内）
    ↓
HTML自動生成 → GitHub Pages に公開

【毎週日曜7時 / GitHub Actions】
data/articles/ から直近7日分のJSONを読み込み
    ↓
ChatGPTで週次まとめ生成
    ↓
weekly.htmlを更新
```

---

## 5. ページ構成（GitHub Pages）

| ページ | 更新タイミング | 内容 |
|--------|----------------|------|
| index.html | 毎日 | 今日のニュース16記事 |
| weekly.html | 毎週日曜 | 週次まとめ |
| archive.html | 毎日 | 過去ニュース一覧 |

### index.html のUI構成
```
🔍 [キーワード検索________]

📈 経済 ／ 🤖 AI事情 ／ 🏠 国内 ／ 🌏 海外  ← タブ切り替え

┌─────────────────────────────────┐
│ 記事タイトル                      │
│ → 2〜3行の要約...                │
│ 📖 用語解説: 「〇〇」とは...      │
│ ❓ クイズ: ？                    │
│         [A] [B] [C] [D]         │
│ 🔗 記事を読む                    │
└─────────────────────────────────┘
```

---

## 6. 使用技術・ツール

| 役割 | ツール | 備考 |
|------|--------|------|
| ニュース取得 | NHK RSS / Google News RSS | 無料・APIキー不要 |
| AI処理 | ChatGPT API（OpenAI） | 要約・用語解説・クイズ生成 |
| データ保存 | JSONファイル（リポジトリ内） | `data/articles/YYYY-MM-DD.json`・外部サービス不要 |
| 公開 | GitHub Pages | 静的HTML |
| 自動実行 | GitHub Actions | cron設定 |
| 検索機能 | JavaScript（クライアントサイド） | サーバー不要 |
| 音声読み上げ | Web Speech API（ブラウザ標準） | APIキー不要 |
| お気に入り | localStorage（ブラウザ標準） | サーバー不要 |
| SNSシェア | 静的URL生成（X / LINE） | APIキー不要 |
| グラフ表示 | Chart.js（CDN） | サーバー不要 |
| 言語 | Python | スクリプト全般 |

---

## 7. 実装フェーズ

| フェーズ | 内容 | 含む機能 |
|----------|------|----------|
| ① コア | RSS取得 → ChatGPT要約 → GitHub Pages公開 | 基本表示・毎日自動更新 |
| ② 拡張 | 用語解説・クイズ・Supabaseアーカイブ | 学習機能・データ蓄積 |
| ③ 仕上げ | キーワード検索・週次まとめ・デザイン整備 | 利便性向上 |
| ④ 追加機能A | 音声読み上げ・お気に入り・SNSシェア・重要度スコア・読了チェック・連続閲覧ストリーク | 拡張UX・習慣化 |
| ⑤ 追加機能B | トレンド可視化・クイズ正答率記録・背景知識の補足・関連過去記事リンク | 理解度深化（データ蓄積後） |
| ⑥ 学習強化 | バッジシステム・一言メモ・昨日の復習・3行日記・フラッシュカード・TTS強化 | 記憶定着・習慣化（2026-05-19 実装済み） |
| ⑦ Voicy化 | 要約を話し言葉スタイル化・TTS前処理強化 | 読み上げ品質向上（2026-05-21 実装済み） |
| ⑧ 基盤刷新 | Supabase廃止→JSONファイル保存、スマホ対応（ボトムナビ・カード1列・余白調整） | 2026-06-01 実装済み |

---

## 8. 必要な環境変数・APIキー

| キー | 用途 |
|------|------|
| OPENAI_API_KEY | ChatGPT API |

> **2026-06-01 更新：** Supabase廃止により `SUPABASE_URL` / `SUPABASE_KEY` は不要。GitHub Secrets から削除してよい。

---

## 10. 確定技術要件

| 項目 | 決定内容 |
|------|----------|
| AIモデル | gpt-4o-mini（全処理共通） |
| HTMLテンプレート | Jinja2（`news-dashboard/templates/` 以下に配置） |
| データ保存 | `data/articles/YYYY-MM-DD.json`（外部DBなし） |
| デプロイ先 | 専用リポジトリ `minamikato54-max/news-dashboard` の `docs/` を GitHub Pages として公開 |
| GitHub Pages URL | `https://minamikato54-max.github.io/news-dashboard/` |
| Pythonバージョン | 3.11 |

### articles JSON スキーマ（`data/articles/YYYY-MM-DD.json`）

```json
[
  {
    "id":         "eco-20260601-01",
    "date":       "2026-06-01",
    "category":   "経済",
    "title":      "記事タイトル",
    "url":        "https://...",
    "summary":    "2〜3行の要約",
    "terms":      [{"word": "用語", "explanation": "説明"}],
    "quiz":       {"question": "問題文", "choices": ["A","B","C","D"], "answer_index": 0},
    "importance": 2,
    "background": "背景知識1〜2行",
    "related_ids": []
  }
]
```

**id 採番ルール：** `{カテゴリプレフィクス}-{YYYYMMDD}-{2桁連番}`
| カテゴリ | プレフィクス |
|----------|-------------|
| 経済 | eco |
| AI事情 | ai |
| 国内 | dom |
| 海外 | int |

---

## 11. 確定機能要件（UI仕様）

| 機能 | 決定内容 |
|------|----------|
| カテゴリUI | タブ切り替え（経済 / AI事情 / 国内 / 海外）。デフォルトは「経済」 |
| クイズ表示 | 記事カード内に常時表示。4択ボタンをクリックで正誤判定 |
| アーカイブ絞り込み | 日付セレクタ ＋ カテゴリボタンで絞り込み |
| 重要度バッジ | カード右上に ★（importance値）を表示。★3は赤枠で強調表示 |
| サイドナビ | PC（641px以上）：固定左サイドバー（幅160px・白背景）。現在ページをハイライト表示 |
| ボトムナビ | スマホ（640px以下）：サイドナビを非表示にし、画面下部に固定ボトムナビバーを表示（今日／アーカイブ／週次） |

### 記事カードの表示構成（index.html）

```
┌─────────────────────────────── ★★★ ┐  ← ★3は赤枠
│ 記事タイトル                          │
│ → 2〜3行の要約...                    │
│ 📖 用語解説: 「〇〇」とは...          │
│ ❓ クイズ:                           │
│         [A] [B] [C] [D]             │
│ 🔍 背景を知る ▼（アコーディオン）      │
│ 📰 関連過去記事（最大2件）             │
│ 🔊 読み上げ  ★ お気に入り  ✓ 読んだ  │
│ 📝 メモ（クリックで展開）             │
│ X でシェア  LINE でシェア             │
│ 🔗 記事を読む                        │
└──────────────────────────────────────┘

右下固定ボタン（FAB）：
  📔（オレンジ）→ 今日の3行日記モーダル
  🃏（紫）　　　→ フラッシュカードモーダル

ページ上部（ヘッダー直下）：
  バッジバー　　→ 獲得済みバッジ一覧（バッジがある場合のみ表示）
  復習セクション → 昨日の間違いクイズ（該当あれば自動表示）
```

---

## 9. ディレクトリ構成

```
news-dashboard/
├── .github/
│   └── workflows/
│       ├── daily.yml       # 毎朝7時実行
│       └── weekly.yml      # 毎週日曜7時実行
├── .gitignore              # .env / .claude / __pycache__ 等を除外
├── scripts/
│   ├── fetch_news.py       # RSS取得（候補20件・ドメイン重複除去）
│   ├── process_articles.py # ChatGPT処理（要約・用語・クイズ・重要度・背景）
│   ├── save_to_json.py     # 記事をJSONファイルに保存（2026-06-01〜）
│   ├── generate_html.py    # HTML生成（Jinja2・カテゴリ上限4件）
│   └── test_render.py      # モックデータでHTMLを検証生成
├── templates/              # Jinja2テンプレート
│   ├── index.html.j2
│   ├── weekly.html.j2
│   └── archive.html.j2
├── data/
│   └── articles/           # 日付ごとの記事JSON（YYYY-MM-DD.json）
├── docs/                   # GitHub Pages公開ディレクトリ
│   ├── index.html
│   ├── weekly.html
│   └── archive.html
├── requirements.txt
└── README.md
```

---

## 12. 品質改善ログ

### 記事数・重複の安定化
| 問題 | 原因 | 対策 |
|------|------|------|
| 16記事に満たない | RSSの先頭4件のみ読み込み、空エントリで不足 | 候補20件読み込み → 有効記事を順番に採用 |
| 同カテゴリ内の内容重複 | Google Newsが同一企業の記事を複数メディアで返す | 1ドメイン1記事に制限（`seen_domains`チェック） |
| 過去記事との重複 | URLが同一でも翌日RSS上位に残る | `save_to_supabase.py`が過去7日のURLを`recent_urls.json`に書き出し、`fetch_news.py`が参照して除外 |
| 1カテゴリ4件超え | 複数ワークフロー実行時に上限超え | `generate_html.py`の`_cap_per_category()`で表示を4件に制限 |

### クイズ品質の改善
| 問題 | 原因 | 対策 |
|------|------|------|
| 選択肢にA/B/C/Dラベルが付く | GPTへのプロンプト例が「選択肢A」形式だった | プロンプト修正 ＋ `strip_choice_labels()`でコード側でも除去 |
| 正答率が常に100%表示 | 記事IDがワークフロー間で変わりlocalStorageと不一致 | クイズ回答時にカテゴリをlocalStorageに保存し、ID非依存で集計 |
| 難易度が低い | 記事の事実を直接問う問題が生成される | プロンプトで「推論・背景知識が必要な問い」に誘導 |

### 音声読み上げの改善
- `preprocessTTS()` で `〔漢字〕力` → `〔漢字〕りょく` に正規表現置換（Unicode漢字範囲 `一-鿿` を利用）
- `loadVoice()` で日本語女性音声を優先選択（Microsoft Haruka 等）
- 読み上げ内容を要約のみ→**要約＋クイズ問題文**に拡張

### 学習強化機能の追加（2026-05-19）
| 機能 | 概要 |
|------|------|
| バッジシステム | 正解累計数に応じてバッジを自動付与、トースト通知 |
| 一言メモ | カードごとに textarea を設置、localStorage に自動保存 |
| 昨日の復習 | 前日の不正解クイズをページ上部に自動表示し再挑戦可能 |
| 今日の3行日記 | FABボタンからモーダルを開き振り返りを記録・履歴閲覧 |
| フラッシュカード | FABボタンから用語解説をランダム出題、覚えた/もう一回で仕分け |

### UI改善（2026-05-19）
| 項目 | 変更内容 |
|------|----------|
| ナビゲーション | ページ下部のリンク行を廃止し、全3テンプレートに固定左サイドバー（`.side-nav`）を追加。幅160px・白背景・右ボーダー。アクティブページは青左ボーダー＋背景でハイライト |
| 国内カテゴリ絵文字 | 🇯🇵（国旗・環境依存で表示不可）→ 🏠 に変更（index.html.j2・archive.html.j2） |
| アーカイブリンク絵文字 | 🗂（U+1F5C2・環境依存）→ 📂 に変更（全3テンプレートのサイドナビ・アーカイブヘッダー） |

### test_render.py の追加（2026-05-19）
- Supabase / OpenAI APIキーなしで全ページ（index・archive・weekly）を検証生成できるスクリプトを追加
- モックデータ：4カテゴリ×4記事=16記事（今日分）＋過去7日×4記事（アーカイブ用）

### RSS・記事数の安定化（2026-05-20）
| 問題 | 原因 | 対策 |
|------|------|------|
| 海外が2件しか取得できない | NHK cat6.xml が当日2件しか配信していない | フォールバックRSS追加（BBC日本語→Google News） |
| カテゴリ横断でURL重複が発生 | NHKが同じ記事を複数カテゴリに掲載 | `fetch_news.py` でグローバル `seen_urls` を全カテゴリ横断で共有 |
| 複数回実行時に5件以上表示される | upsertで異なるURLが追加蓄積される | `generate_html.py` にカテゴリごと最大4件キャップを追加（`ARTICLES_PER_CATEGORY = 4`） |

### Supabase RLS・upsert修正（2026-05-20）
| 問題 | 原因 | 対策 |
|------|------|------|
| INSERT拒否エラー（code 42501） | RLSポリシーがanon/authenticatedのINSERTを未許可 | Supabaseで `articles_insert`・`articles_update` ポリシーを追加 |
| upsert時に行の二重更新エラー | バッチupsertで同一URLが複数存在 | `save_to_supabase.py` を1件ずつupsertに変更 + URL重複排除 |
| ON CONFLICTターゲット未設定 | `(url, date)` にUNIQUE制約がなかった | Supabaseに `CONSTRAINT articles_url_date_unique UNIQUE (url, date)` を追加 |

### アーカイブページ空欄バグ修正（2026-05-21）
| 問題 | 原因 | 対策 |
|------|------|------|
| archive.html を開いても記事が表示されない | Jinja2 `autoescape=True` により `articles_json` の `"` が `&#34;` にHTMLエスケープされJavaScriptが壊れる | `archive.html.j2` の `{{ articles_json }}` を `{{ articles_json \| safe }}` に変更 |

### 「全記事を聴く」ボタン削除（2026-05-25）
| 対象 | 変更内容 |
|------|---------|
| `templates/index.html.j2` | `#briefing-btn` CSS・ボタンHTML・`MORNING_ARTICLES`変数・`startMorningBriefing` / `playBriefingQueue` / `speakThen` / `speakSequence` / `showBriefingNext` / `hideBriefingNext` / `advanceBriefing` / `switchToCategory` 関数を全削除 |
| `scripts/generate_html.py` | `briefing_articles` リスト生成・テンプレートへの引数渡しを削除 |
| `docs/index.html` | 上記に対応する生成済みHTMLも直接修正 |

既存の記事ごと「🔊 読み上げ」ボタンは変更なし。

### タイムゾーン修正（2026-05-25 対応済み）

**問題：** GitHub Actions ランナーが UTC 動作のため `date.today()` がUTC日付を返し、ページタイトルが常に「前日」の日付になっていた。

**対応：** `.github/workflows/daily.yml` に `TZ: Asia/Tokyo` を追加。

```yaml
env:
  TZ: Asia/Tokyo   # 追加済み
```

これにより全Pythonスクリプトの `date.today()` が自動的にJST日付を返す。

| タイミング | JST | `date.today()` | ページタイトル |
|-----------|-----|----------------|--------------|
| 修正前 | 05/26 07:00 | 2026-05-25（UTC） | 「5/25 の最新ニュース」❌ |
| **修正後** | **05/26 07:00** | **2026-05-26（JST）** | **「5/26 の最新ニュース」** ✅ |

毎朝7時の自動実行で**当日JST日付**が表示される。手動実行は不要。

**検証済み（2026-05-25）：** 手動実行ログにて `Supabase に 16 件保存完了（日付: 2026-05-25）` を確認。修正前は `2026-05-24`（UTC前日）だったのが、JST当日の日付に変わった。

**ワークフロー正常完了の目安：**
- 経済・国内・海外・AI事情 各4件取得（計16件）
- Supabase に 16 件保存完了（日付がJST当日になっていることを確認）
- index.html 生成完了（16件）
- archive.html 生成完了（累積件数）

### Supabase廃止 → JSONファイル保存方式に移行（2026-06-01）

**背景：** Supabase無料枠終了により `[Errno -2] Name or service not known` が発生し、5/30以降ダッシュボードの更新が停止した。

| 変更内容 | 詳細 |
|----------|------|
| `save_to_supabase.py` 廃止 | `save_to_json.py` に置換。`data/articles/YYYY-MM-DD.json` に保存 |
| `generate_html.py` 修正 | Supabaseクエリ → ローカルJSON読み込みに変更 |
| `weekly.py` 修正 | Supabaseクエリ・`update_related_ids` 削除、JSONファイル読み込みに変更 |
| ワークフロー更新 | `daily.yml` / `weekly.yml` から `SUPABASE_URL` / `SUPABASE_KEY` 削除 |
| `requirements.txt` 更新 | `supabase` パッケージ削除 |
| 関連記事リンク廃止 | `related_ids` 更新処理はSupabase依存のため削除 |

### スマホ（モバイル）対応（2026-06-01）

**背景：** サイドナビ（`margin-left: 160px`）がスマホ画面に固定されたままで、コンテンツが押し潰されて見づらかった。

| 変更内容 | 詳細 |
|----------|------|
| ボトムナビ追加 | 全3テンプレートに `.mobile-nav` を追加。画面下部固定・今日/アーカイブ/週次の3リンク |
| サイドナビ非表示 | `@media (max-width: 640px)` で `.side-nav { display: none }` |
| `margin-left` 除去 | スマホで `body { margin-left: 0 !important }` |
| カード1列化 | `cards-grid` / archive `cards` を `grid-template-columns: 1fr` に |
| 余白縮小 | ヘッダー・タブ・カードセクションのパディングをスマホ向けに縮小 |
| FAB位置調整 | `diary-fab` / `fc-fab` をボトムナビの上（`bottom: 76px` / `140px`）に移動 |
| iPhone対応 | `env(safe-area-inset-bottom)` でホームインジケーター領域を考慮 |

**クイズ履歴について：** localStorageはデバイス単位のため、PC側の履歴はスマホには引き継がれない。スマホで使い始めると新たに蓄積される。

### Voicy化改善（2026-05-21）
| 改善内容 | 対象ファイル | 変更内容 |
|----------|-------------|---------|
| 要約を話し言葉スタイルに変更 | `scripts/process_articles.py` | `SYSTEM_PROMPT` のsummary指示を「です・ます調・口語表現」に変更。「〜である」「〜とされる」などの文語禁止 |
| TTS前処理強化 | `templates/index.html.j2` | `preprocessTTS()` に括弧→ポーズ・中点→ポーズ・%→パーセント・矢印→日本語 の変換を追加 |

### クイズ難易度強化（2026-05-20）
- `process_articles.py` のシステムプロンプトに難易度ルールを追加
  - 「記事によると〜は何か」の単純読み取り問題を禁止
  - 数値・固有名詞・因果関係・背景知識を問う問題を指定
  - 誤答3つを「一見もっともらしい内容」に統一
  - 「なぜ〜か」「〜の目的は」など考察が必要な問い方を指定

### GitHub デプロイ整備（2026-05-19）
- `news-dashboard/` 内で `git init` を実行し、独立した git リポジトリとして初期化
- リモートを `minamikato54-max/news-dashboard` に設定し全ファイルをプッシュ
- `.gitignore` を新規作成（除外対象：`.env` / `.claude/` / `__pycache__/` / `*.pyc` / 中間生成ファイル）
- GitHub Secrets（`OPENAI_API_KEY` / `SUPABASE_URL` / `SUPABASE_KEY`）は設定済みであることを確認
- GitHub Actions（daily.yml）が毎朝 UTC 22:00（JST 7:00）に自動実行され `docs/` を更新・プッシュする構成を確認
- 手動実行コマンド：`gh workflow run daily.yml --repo minamikato54-max/news-dashboard`

---

## 13. 今後の展望

### クイズ履歴のデバイス間同期（検討中）
現状はlocalStorageのため、スマホ・PCで記録が分離する。以下の方法で解消できる。

| 方法 | 難易度 | 備考 |
|------|--------|------|
| エクスポート/インポート機能 | 低 | JSONファイルをダウンロード→別ブラウザで読み込む。外部サービス不要 |
| 無料DBサービスで保存 | 中 | PlanetScale・Turso・Neon等（要調査） |

---

## 14. 公開情報

| 項目 | 内容 |
|------|------|
| 公開URL | `https://minamikato54-max.github.io/news-dashboard/` |
| リポジトリ | `minamikato54-max/news-dashboard`（GitHub） |
| 更新タイミング | 毎朝7時（JST）自動更新 |
| 対象者 | 誰でも閲覧・クイズ回答可（ログイン不要） |
| クイズ結果保存 | ブラウザのlocalStorage（デバイス単位） |
