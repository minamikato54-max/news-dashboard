# ニュースダッシュボード 仕様書

作成日：2026-05-03

---

## 1. プロジェクト概要

複数のニュースソースから日本語ニュースを自動収集し、AI要約・用語解説・クイズ付きで毎朝GitHub Pagesに公開する自動化システム。課題共有にも利用できるよう、URLひとつで誰でも閲覧可能にする。

---

## 2. 要件

### 基本要件
- 毎朝7時に自動実行
- 日本語ニュースのみ
- 4カテゴリ × 4記事 = 計16記事/日
- 記事タイトル・要約・元記事リンクを表示
- GitHub Pages で公開（ログイン不要・URLのみで閲覧可）

### カテゴリ
| カテゴリ | ニュースソース |
|----------|----------------|
| 経済 | NHK経済RSS |
| AI事情 | Google News RSS（「AI 人工知能」検索） |
| 国内 | NHK国内ニュースRSS |
| 海外 | NHK国際ニュースRSS |

---

## 3. 機能一覧

### 3-1. 毎日更新機能（コア）
- RSSフィードから各カテゴリ4記事を取得
- ChatGPT APIで各記事を2〜3行で要約
- 結果をGitHub Pagesに自動反映

### 3-2. 用語解説
- 各記事中の難しい単語・専門用語をChatGPTが自動抽出
- 簡単な説明文を生成して記事に付記

### 3-3. クイズ機能
- 各記事の内容をもとにChatGPTが4択クイズを自動生成
- ページ内でインタラクティブに回答できる（JavaScript）

### 3-4. キーワード検索フィルター
- ページ内の検索ボックスにキーワードを入力すると即時フィルタリング
- クライアントサイドJavaScriptで実装（サーバー不要）

### 3-5. アーカイブ機能
- 毎日の記事データをSupabaseに保存
- archive.htmlで過去のニュースを日付・カテゴリで閲覧可能

### 3-6. 週次まとめ
- 毎週日曜7時に自動実行
- Supabaseから1週間分の記事を取得
- ChatGPTで週の重要ニュースをまとめた要約を生成
- weekly.htmlに反映

### 3-7. 音声読み上げ
- 記事ごとに「🔊 読み上げ」ボタンを設置
- ブラウザ標準の Web Speech API（SpeechSynthesis）で実装（外部APIキー不要）
- 読み上げ順：**要約 → 「クイズです。〔問題文〕」**（クイズがある記事のみ）
- 読み上げ中はボタンを「⏹ 停止」に切り替え。クリックで即時停止
- `preprocessTTS()` 関数で漢字の読み誤りを補正
  - `〔漢字〕力` → `〔漢字〕りょく`（例：競争力→きょうそうりょく、影響力→えいきょうりょく）
- `loadVoice()` 関数で日本語女性音声を優先選択
  - 選択順：Microsoft Haruka → Google 日本語 → Kyoko → O-Ren → 任意の ja-JP 音声
  - 読み上げ速度：0.95（やや遅め・聞き取りやすさ重視）
- `speechSynthesis.onvoiceschanged` で音声一覧の非同期読み込みに対応

### 3-8. お気に入り保存
- 記事ごとに「お気に入り」ボタン（★）を設置
- ブラウザの localStorage に記事IDを保存（サーバー不要）
- お気に入り一覧をフィルタリングで表示できる

### 3-9. SNSシェア
- 記事ごとにX（Twitter）・LINEへのシェアボタンを設置
- 記事タイトル＋GitHubPagesのURLをパラメータに埋め込んだリンクを生成
- 静的リンクのみで実装（JavaScript不要）

### 3-10. トレンドワード可視化
- Supabaseに蓄積された1週間分の記事タイトル・要約からキーワードを集計
- ChatGPTで上位トレンドワードを抽出
- weekly.html内に横棒グラフ（Chart.js）で表示

### 3-11. 連続閲覧ストリーク
- ページを開いた日付をlocalStorageに記録
- 連続で開いた日数をページ上部に表示（例：「🔥 3日連続閲覧中！」）
- 1日でも途切れるとリセット

### 3-12. 読了チェック
- 記事ごとに「読んだ」ボタンを設置
- チェック済み記事はlocalStorageに保存
- 今日の16記事のうち何記事読んだか進捗バーをページ上部に表示
- 翌日（日付が変わったタイミング）に自動リセット

### 3-13. 重要度スコア
- ChatGPT APIで各記事に重要度（★1〜3）を自動判定
- 記事カードに重要度バッジを表示
- ★3の記事は視覚的に強調表示

### 3-14. クイズ正答率の記録
- クイズの正解・不正解をlocalStorageに日付・カテゴリ付きで保存（`nd_quiz_YYYY-MM-DD`）
- archive.html最上部にカテゴリ別折れ線グラフ（Chart.js）で日別推移を表示
- グラフ上部に本日の正答率をカテゴリ別テキストで表示
- クイズ回答時にカテゴリをlocalStorageに一緒に保存することで、記事IDに依存しない正確な集計を実現

### 3-15. 背景知識の補足
- 記事ごとに「背景を知る」ボタンを設置（デフォルト非表示）
- ボタンを押したときのみChatGPT生成の背景知識（1〜2行）をアコーディオン展開
- 生成内容はHTML生成時に埋め込み済み（ページロード後の追加API呼び出しなし）

### 3-17. バッジシステム
- クイズ正解累計数に応じてバッジを自動付与（localStorage に保存）
- バッジ獲得時にトースト通知（画面上部に3秒表示）
- 獲得済みバッジはページ上部のバッジバーに常時表示

| 正解数 | バッジ |
|--------|--------|
| 1問 | 🎯 初正解 |
| 5問 | 📚 5問正解 |
| 10問 | ⭐ 10問達成 |
| 20問 | 🌟 20問達成 |
| 50問 | 👑 クイズ王 |

- localStorage キー：`nd_badges`（獲得バッジ名の配列）

### 3-18. 一言メモ
- 各記事カードに「📝 メモ」ボタンを設置
- クリックで textarea が展開（再クリックで折りたたみ）
- 入力と同時に `localStorage` に自動保存（`nd_memo_<記事ID>`）
- メモ保存済みの場合はボタンラベルが「📝 メモあり ✓」に変わる
- メモ内容は記事IDに紐づくため、記事が変わる翌日以降は別のキーとして蓄積

### 3-19. 昨日の復習
- ページロード時に前日間違えたクイズを自動検出し、ページ上部（タブより上）に表示
- 表示条件：前日の `nd_quiz_DATE` に `ok:false` のエントリが存在する場合
- 復習カードで再回答可能（正誤フィードバックあり・バッジ集計対象外）

**データ保存（クイズ回答時に自動実行）：**
- `nd_quiz_DATE`：正解/不正解の記録（既存）
- `nd_quiz_detail_DATE`：復習用の問題詳細（question / choices / answer_index / category / title）

### 3-20. 今日の3行日記
- 右下固定の「📔」FABボタン（オレンジ）でモーダルを開く
- 3つのテキストエリアに沿って振り返りを記録

| # | 入力欄 |
|---|--------|
| 1 | 今日一番印象に残ったニュースは？ |
| 2 | そのニュースについて感じたこと・考えたことは？ |
| 3 | 明日確認したいこと・もっと知りたいことは？ |

- 「💾 保存」で `nd_diary_DATE` に保存（JSON配列）
- モーダル内に過去5日分の日記履歴を表示

### 3-21. フラッシュカード
- 右下固定の「🃏」FABボタン（紫）でモーダルを開く
- 今日の全記事の用語解説（`terms`）をDOMから収集・シャッフルして出題
- カード表面：単語、クリックで裏面（解説）にフリップ
- 「✅ 覚えた」→ 次のカードへ進む
- 「🔄 もう一回」→ 現在のカードを末尾に移動して再出題
- 全カード終了時に完了メッセージを表示
- 用語が0件の日は「用語解説がありません」とアラート表示

### 3-16. 関連過去記事リンク
- Supabaseのアーカイブから同カテゴリ・類似キーワードの過去記事を自動抽出
- 記事カード下部に「関連記事」として最大2件リンク表示
- weekly.yml実行時にChatGPTで類似度を判定して紐付け

---

## 4. システム構成

```
【毎朝7時 / GitHub Actions】
RSS取得（NHK + Google News）
    ↓
ChatGPT API（記事ごとに一括処理）
  ├─ 2〜3行要約
  ├─ 用語解説（難しい単語を自動抽出・説明）
  └─ クイズ問題生成（4択）
    ↓
Supabase に保存（アーカイブ）
    ↓
HTML自動生成 → GitHub Pages に公開

【毎週日曜7時 / GitHub Actions】
Supabaseから1週間分取得
    ↓
ChatGPTで週次まとめ生成
    ↓
weekly.htmlを更新
```

---

## 5. ページ構成（GitHub Pages）

| ページ | 更新タイミング | 内容 |
|--------|----------------|------|
| index.html | 毎日 | 今日のニュース16記事 |
| weekly.html | 毎週日曜 | 週次まとめ |
| archive.html | 毎日 | 過去ニュース一覧 |

### index.html のUI構成
```
🔍 [キーワード検索________]

📈 経済 ／ 🤖 AI事情 ／ 🏠 国内 ／ 🌏 海外  ← タブ切り替え

┌─────────────────────────────────┐
│ 記事タイトル                      │
│ → 2〜3行の要約...                │
│ 📖 用語解説: 「〇〇」とは...      │
│ ❓ クイズ: ？                    │
│         [A] [B] [C] [D]         │
│ 🔗 記事を読む                    │
└─────────────────────────────────┘
```

---

## 6. 使用技術・ツール

| 役割 | ツール | 備考 |
|------|--------|------|
| ニュース取得 | NHK RSS / Google News RSS | 無料・APIキー不要 |
| AI処理 | ChatGPT API（OpenAI） | 要約・用語解説・クイズ生成 |
| データ保存 | Supabase | アーカイブ用 |
| 公開 | GitHub Pages | 静的HTML |
| 自動実行 | GitHub Actions | cron設定 |
| 検索機能 | JavaScript（クライアントサイド） | サーバー不要 |
| 音声読み上げ | Web Speech API（ブラウザ標準） | APIキー不要 |
| お気に入り | localStorage（ブラウザ標準） | サーバー不要 |
| SNSシェア | 静的URL生成（X / LINE） | APIキー不要 |
| グラフ表示 | Chart.js（CDN） | サーバー不要 |
| 言語 | Python | スクリプト全般 |

---

## 7. 実装フェーズ

| フェーズ | 内容 | 含む機能 |
|----------|------|----------|
| ① コア | RSS取得 → ChatGPT要約 → GitHub Pages公開 | 基本表示・毎日自動更新 |
| ② 拡張 | 用語解説・クイズ・Supabaseアーカイブ | 学習機能・データ蓄積 |
| ③ 仕上げ | キーワード検索・週次まとめ・デザイン整備 | 利便性向上 |
| ④ 追加機能A | 音声読み上げ・お気に入り・SNSシェア・重要度スコア・読了チェック・連続閲覧ストリーク | 拡張UX・習慣化 |
| ⑤ 追加機能B | トレンド可視化・クイズ正答率記録・背景知識の補足・関連過去記事リンク | 理解度深化（データ蓄積後） |
| ⑥ 学習強化 | バッジシステム・一言メモ・昨日の復習・3行日記・フラッシュカード・TTS強化 | 記憶定着・習慣化（2026-05-19 実装済み） |

---

## 8. 必要な環境変数・APIキー

| キー | 用途 |
|------|------|
| OPENAI_API_KEY | ChatGPT API |
| SUPABASE_URL | Supabase接続 |
| SUPABASE_KEY | Supabase認証 |

---

## 10. 確定技術要件

| 項目 | 決定内容 |
|------|----------|
| AIモデル | gpt-4o-mini（全処理共通） |
| HTMLテンプレート | Jinja2（`news-dashboard/templates/` 以下に配置） |
| DB設計 | Supabase 単一テーブル（`articles`） |
| デプロイ先 | 専用リポジトリ `minamikato54-max/news-dashboard` の `docs/` を GitHub Pages として公開 |
| GitHub Pages URL | `https://minamikato54-max.github.io/news-dashboard/` |
| Pythonバージョン | 3.11 |

### Supabase articles テーブル定義

```sql
create table articles (
  id          uuid primary key default gen_random_uuid(),
  date        date not null,
  category    text not null,  -- '経済' | 'AI事情' | '国内' | '海外'
  title       text not null,
  url         text not null,
  summary     text not null,
  terms       jsonb,          -- [{word, explanation}, ...]
  quiz        jsonb,          -- {question, choices[4], answer_index}
  importance  int,            -- 1〜3
  background  text,
  related_ids jsonb           -- [uuid, ...]
);
```

---

## 11. 確定機能要件（UI仕様）

| 機能 | 決定内容 |
|------|----------|
| カテゴリUI | タブ切り替え（経済 / AI事情 / 国内 / 海外）。デフォルトは「経済」 |
| クイズ表示 | 記事カード内に常時表示。4択ボタンをクリックで正誤判定 |
| アーカイブ絞り込み | 日付セレクタ ＋ カテゴリボタンで絞り込み |
| 重要度バッジ | カード右上に ★（importance値）を表示。★3は赤枠で強調表示 |
| サイドナビ | 全3ページ共通の固定左サイドバー（幅160px・白背景）。現在ページをハイライト表示 |

### 記事カードの表示構成（index.html）

```
┌─────────────────────────────── ★★★ ┐  ← ★3は赤枠
│ 記事タイトル                          │
│ → 2〜3行の要約...                    │
│ 📖 用語解説: 「〇〇」とは...          │
│ ❓ クイズ:                           │
│         [A] [B] [C] [D]             │
│ 🔍 背景を知る ▼（アコーディオン）      │
│ 📰 関連過去記事（最大2件）             │
│ 🔊 読み上げ  ★ お気に入り  ✓ 読んだ  │
│ 📝 メモ（クリックで展開）             │
│ X でシェア  LINE でシェア             │
│ 🔗 記事を読む                        │
└──────────────────────────────────────┘

右下固定ボタン（FAB）：
  📔（オレンジ）→ 今日の3行日記モーダル
  🃏（紫）　　　→ フラッシュカードモーダル

ページ上部（ヘッダー直下）：
  バッジバー　　→ 獲得済みバッジ一覧（バッジがある場合のみ表示）
  復習セクション → 昨日の間違いクイズ（該当あれば自動表示）
```

---

## 9. ディレクトリ構成（予定）

```
news-dashboard/
├── .github/
│   └── workflows/
│       ├── daily.yml       # 毎朝7時実行
│       └── weekly.yml      # 毎週日曜7時実行
├── scripts/
│   ├── fetch_news.py       # RSS取得（候補20件・ドメイン重複除去）
│   ├── process_articles.py # ChatGPT処理（要約・用語・クイズ・重要度・背景）
│   ├── save_to_supabase.py # アーカイブ保存（過去7日URL重複チェック）
│   ├── generate_html.py    # HTML生成（Jinja2・カテゴリ上限4件）
│   └── test_render.py      # Supabase不要のモックデータでHTMLを検証生成
├── templates/              # Jinja2テンプレート
│   ├── index.html.j2
│   ├── weekly.html.j2
│   └── archive.html.j2
├── docs/                   # GitHub Pages公開ディレクトリ
│   ├── index.html
│   ├── weekly.html
│   └── archive.html
├── recent_urls.json        # 過去7日のURL一覧（重複チェック用・自動生成）
├── requirements.txt
└── README.md
```

---

## 12. 品質改善ログ

### 記事数・重複の安定化
| 問題 | 原因 | 対策 |
|------|------|------|
| 16記事に満たない | RSSの先頭4件のみ読み込み、空エントリで不足 | 候補20件読み込み → 有効記事を順番に採用 |
| 同カテゴリ内の内容重複 | Google Newsが同一企業の記事を複数メディアで返す | 1ドメイン1記事に制限（`seen_domains`チェック） |
| 過去記事との重複 | URLが同一でも翌日RSS上位に残る | `save_to_supabase.py`が過去7日のURLを`recent_urls.json`に書き出し、`fetch_news.py`が参照して除外 |
| 1カテゴリ4件超え | 複数ワークフロー実行時に上限超え | `generate_html.py`の`_cap_per_category()`で表示を4件に制限 |

### クイズ品質の改善
| 問題 | 原因 | 対策 |
|------|------|------|
| 選択肢にA/B/C/Dラベルが付く | GPTへのプロンプト例が「選択肢A」形式だった | プロンプト修正 ＋ `strip_choice_labels()`でコード側でも除去 |
| 正答率が常に100%表示 | 記事IDがワークフロー間で変わりlocalStorageと不一致 | クイズ回答時にカテゴリをlocalStorageに保存し、ID非依存で集計 |
| 難易度が低い | 記事の事実を直接問う問題が生成される | プロンプトで「推論・背景知識が必要な問い」に誘導 |

### 音声読み上げの改善
- `preprocessTTS()` で `〔漢字〕力` → `〔漢字〕りょく` に正規表現置換（Unicode漢字範囲 `一-鿿` を利用）
- `loadVoice()` で日本語女性音声を優先選択（Microsoft Haruka 等）
- 読み上げ内容を要約のみ→**要約＋クイズ問題文**に拡張

### 学習強化機能の追加（2026-05-19）
| 機能 | 概要 |
|------|------|
| バッジシステム | 正解累計数に応じてバッジを自動付与、トースト通知 |
| 一言メモ | カードごとに textarea を設置、localStorage に自動保存 |
| 昨日の復習 | 前日の不正解クイズをページ上部に自動表示し再挑戦可能 |
| 今日の3行日記 | FABボタンからモーダルを開き振り返りを記録・履歴閲覧 |
| フラッシュカード | FABボタンから用語解説をランダム出題、覚えた/もう一回で仕分け |

### UI改善（2026-05-19）
| 項目 | 変更内容 |
|------|----------|
| ナビゲーション | ページ下部のリンク行を廃止し、全3テンプレートに固定左サイドバー（`.side-nav`）を追加。幅160px・白背景・右ボーダー。アクティブページは青左ボーダー＋背景でハイライト |
| 国内カテゴリ絵文字 | 🇯🇵（国旗・環境依存で表示不可）→ 🏠 に変更（index.html.j2・archive.html.j2） |
| アーカイブリンク絵文字 | 🗂（U+1F5C2・環境依存）→ 📂 に変更（全3テンプレートのサイドナビ・アーカイブヘッダー） |

### test_render.py の追加（2026-05-19）
- Supabase / OpenAI APIキーなしで全ページ（index・archive・weekly）を検証生成できるスクリプトを追加
- モックデータ：4カテゴリ×4記事=16記事（今日分）＋過去7日×4記事（アーカイブ用）

---

## 13. 今後の展望

### Supabase Auth によるユーザー認証（検討中）
静的サイト（GitHub Pages）のまま Supabase JS クライアントで実装可能。

| 項目 | 内容 |
|------|------|
| 認証方式 | マジックリンク（メールアドレス入力→メール認証→ログイン完了・パスワード不要） |
| クイズ結果 | 未ログイン：localStorage / ログイン済み：Supabase `quiz_results` テーブル |
| 対象ユーザー | 知人・AIプログラミング講習の受講生・講師 |
| メリット | 複数デバイスでも学習記録が継続・グラフが積み上がる |

```sql
create table quiz_results (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid references auth.users not null,
  article_date date not null,
  article_id  text not null,
  category    text not null,
  ok          boolean not null,
  answered_at timestamptz default now(),
  unique(user_id, article_id)
);
```

---

## 14. 公開情報

| 項目 | 内容 |
|------|------|
| 公開URL | `https://minamikato54-max.github.io/news-dashboard/` |
| リポジトリ | `minamikato54-max/news-dashboard`（GitHub） |
| 更新タイミング | 毎朝7時（JST）自動更新 |
| 対象者 | 誰でも閲覧・クイズ回答可（ログイン不要） |
| クイズ結果保存 | ブラウザのlocalStorage（デバイス単位） |
